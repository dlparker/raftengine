
1. [] Add configuration for lmdb to control key parameters such as memory
2. [] See if using jsonpickle makes lmdb faster
3. [] Both sqlite and lmdb currently re-read the log rec after saving, see if removing
   that speeds things up.
4. [] Add timeout control to all the rpc and raft clients in examples/simple
5. [] Test example/simple with regular timings. Ensure it has no effect on performance numbers
      (heartbeats should not happend in busy system, timeouts either).
   
   



   
   
	 
	 
 




